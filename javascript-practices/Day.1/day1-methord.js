// day 1
// .console log , typeof

let details = {
  name: "muhammed faris",
  place: "vavad-koduvally",
  age: 22,
};

let numbers = [1, 2, 3, 45, 69];

let name = "muhammed faris";

let count = 50000;

let arrays = ["string", 500, null];

let type = typeof array;

console.log(type);

let array = [1, 2, 5, 4];

let checkArry = Array.isArray(array);
console.log(checkArry);

// so basically array is a special type of object it coduction / hold string number boolean undefined etc
// if must check is it array or anything else use Array.isArray()
